## 1. Implementation

- [x] 1.1 Create `SettingsSection` component with title prop and children container
- [x] 1.2 Update `SettingsView` to wrap settings in `SettingsSection` with title "General"

## 2. Verification

- [x] 2.1 Run `wails dev -tags webkit2_41` and verify "General" section header displays
- [x] 2.2 Verify all four settings render correctly inside the section
- [x] 2.3 Verify API Key field still conditionally appears based on provider
